//
// import 'react-elm' as Elm;
// import '../elm/my-stuff.js' as Elm
//
// <Elm src={Elm.MyStuff} flags={42} ports={setupPorts} />
//

var app = app || {};

(function () {

'use strict';

var TodoItem = React.createClass({

	getInitialState: function () {
		this.props.src;
		this.props.flags
		return;
	},

	shouldComponentUpdate: function (nextProps, nextState) {
	},

	componentDidUpdate: function (prevProps) {
	},

	render: function () {
	}

});

})();
